{
  "name": "bad-debt-tycoon",
  "private": true,
  "author": "Naomi Hauret",
  "scripts": {
    "check": "biome check --write",
    "build:contracts": "cd packages/contracts && bun run build",
    "build": "bun --filter '*' build",
    "dev": "bun --filter '*' dev"
  },
  "workspaces": [
    "distributions/*",
    "apps/*",
    "engine/*",
    "packages/*",
    "tooling/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.5"
  }
}
