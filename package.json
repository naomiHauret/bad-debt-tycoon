{
  "name": "crippling-debt-tycoon",
  "private": true,
  "author": "Naomi Hauret",
  "scripts": {
    "check": "biome check --write",
    "build:engine": "cd packages/engine && bun run build",
    "build:game": "cd packages/game && bun run build",
    "dev": "bun --filter '*' dev"
  },
  "workspaces": [
    "distributions/*",
    "apps/*",
    "engine/*",
    "packages/*",
    "tooling/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.5"
  }
}
